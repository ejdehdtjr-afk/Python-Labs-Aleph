# 개인 키 설정 안내

코드의 `YOUR_VALUE_HERE`, `각자_키를_넣으세요`, `YOUR_USER`, `YOUR_PASSWORD`, `각자_DB_비밀번호를_넣으세요`, `YOUR_N8N_WEBHOOK_URL`, `각자_웹훅_URL을_넣으세요`에는 각자 발급받은 키·비밀번호·URL을 입력하세요.

환경변수를 사용하는 예제는 `.env.example`을 `.env`로 복사한 후 값을 입력하세요. 기존 연습 코드에 값이 직접 지정된 경우 해당 위치에 본인 값을 입력하세요. 실제 값은 Git에 커밋하지 마세요.

API 키: 공공데이터포털 또는 사용하는 LLM 서비스에서 개인 키 발급. DB: 본인 MySQL 계정과 비밀번호. JWT/보안 이벤트 키: 각자 충분히 긴 임의 문자열 생성. 웹훅: 본인 n8n 워크플로 URL 사용.

일반 코드와 학습 자료는 포함하고 실제 `.env`, 가상환경, 캐시, 실행 로그, 로컬 DB 및 도구 상태는 제외했습니다. 노트북 실행 출력은 키가 남지 않도록 비웠습니다. 게시판의 명시적인 실습 계정 비밀번호는 테스트용 예시입니다.

이전에 GitHub에 올라갔던 실제 키는 제공처에서 폐기·재발급하세요. 새 이력으로 교체해도 기존 커밋 링크나 다른 복제본의 완전한 삭제는 보장되지 않습니다.
